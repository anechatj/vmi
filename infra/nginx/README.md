# infra/nginx

Reverse proxy config — ใช้เฉพาะตอน deploy SIT (ดู `infra/docker/docker-compose.sit.yml`)

ยังไม่ต้องใช้ตอน local dev — local เข้าตรงที่ port ของแต่ละ service (web, policy-api, minio, keycloak)
